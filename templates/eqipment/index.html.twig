{% extends 'base.html.twig' %}

{% block title %}Eqipment index{% endblock %}

{% block body %}
    <h1>Eqipment index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for eqipment in eqipments %}
            <tr>
                <td>{{ eqipment.id }}</td>
                <td>{{ eqipment.name }}</td>
                <td>{{ eqipment.description }}</td>
                <td>
                    <a href="{{ path('app_eqipment_show', {'id': eqipment.id}) }}">show</a>
                    <a href="{{ path('app_eqipment_edit', {'id': eqipment.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_eqipment_new') }}">Create new</a>
{% endblock %}
