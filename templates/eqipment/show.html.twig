{% extends 'base.html.twig' %}

{% block title %}Eqipment{% endblock %}

{% block body %}
    <h1>Eqipment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ eqipment.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ eqipment.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ eqipment.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_eqipment_index') }}">back to list</a>

    <a href="{{ path('app_eqipment_edit', {'id': eqipment.id}) }}">edit</a>

    {{ include('eqipment/_delete_form.html.twig') }}
{% endblock %}
