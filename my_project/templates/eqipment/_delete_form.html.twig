<form method="post" action="{{ path('app_eqipment_delete', {'id': eqipment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eqipment.id) }}">
    <button class="btn">Delete</button>
</form>
