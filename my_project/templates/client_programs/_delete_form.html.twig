<form method="post" action="{{ path('app_client_programs_delete', {'id': client_program.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client_program.id) }}">
    <button class="btn">Delete</button>
</form>
